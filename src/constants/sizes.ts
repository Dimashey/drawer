export enum Size {
  Small = "Small",
  Medium = "Medium",
  Large = "Large",
}

export enum LineType {
  Direct = "Direct",
  Parallel = "Parallel",
}

export const SIZES_NAMES = Object.values(Size);
export const LINES_TYPES_NAMES = Object.values(LineType);
